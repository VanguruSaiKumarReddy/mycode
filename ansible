- name: "CIS 4.2.3 | Secure /etc/cron.allow (root:root 0600)"
  file:
    path: /etc/cron.allow
    owner: root
    group: root
    mode: '0600'
    state: file
  tags: ['cis_4_2_3','cis','cron']


stat -c "%U:%G %a %n" /etc/cron.allow
# Expect: root:root 600 /etc/cron.allow
